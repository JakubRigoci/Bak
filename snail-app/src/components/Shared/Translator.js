export const translate = (eventType) => {
    switch (eventType) {
        case "snail":
            return "SNEK"
        case "box":
            return "BOX"
        case "snuska":
            return "SNUSKA"
        case "group":
            return "SKUPINA"
    }
}